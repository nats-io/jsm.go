{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nats.io/schemas/server/monitor/v1/ipqueuesz_request.json",
  "description": "A request to the NATS $SYS monitoring endpoint for IP queue information",
  "title": "io.nats.server.monitor.v1.ipqueuesz_request",
  "type": "object",
  "properties": {
    "all": {
      "type": "boolean",
      "description": "Include all IP queues"
    },
    "filter": {
      "type": "string",
      "description": "Filter IP queues by pattern"
    }
  }
}
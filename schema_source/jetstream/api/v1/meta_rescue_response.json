{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nats.io/schemas/jetstream/api/v1/meta_rescue_response.json",
  "description": "A response from the JetStream $JS.API.META.RESCUE API",
  "title": "io.nats.jetstream.api.v1.meta_rescue_response",
  "type": "object",
  "oneOf": [
    {
      "$ref": "definitions.json#/definitions/error_response"
    },
    {
      "required": ["server", "server_id"],
      "type": "object",
      "not": {
        "required": ["error"]
      },
      "properties": {
        "server": {
          "type": "string",
          "description": "The server name of the responding server"
        },
        "server_id": {
          "type": "string",
          "description": "The server ID of the responding server"
        },
        "prev_quorum": {
          "type": "integer",
          "description": "The effective quorum before the rescue was applied"
        },
        "new_quorum": {
          "type": "integer",
          "description": "The effective quorum after the rescue was applied"
        }
      }
    }
  ],
  "properties": {
    "type": {
      "type": "string",
      "const": "io.nats.jetstream.api.v1.meta_rescue_response"
    }
  }
}

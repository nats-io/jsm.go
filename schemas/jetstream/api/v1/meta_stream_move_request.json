{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nats.io/schemas/jetstream/api/v1/meta_stream_move_request.json",
  "description": "A request to the JetStream $JS.API.SERVER.STREAM.MOVE",
  "title": "io.nats.jetstream.api.v1.meta_stream_move_request",
  "type": "object",
  "required": [
    "server",
    "account",
    "stream"
  ],
  "properties": {
    "server": {
      "type": "string",
      "description": "Server where the stream should be evacuated from"
    },
    "account": {
      "type": "string",
      "description": "The account where the stream is hosted in"
    },
    "stream": {
      "type": "string",
      "description": "The name of the stream to move"
    },
    "tags": {
      "type": "array",
      "description": "Placement tags for the move, will not persist to the stream config",
      "items": {
        "type": "string"
      }
    }
  }
}

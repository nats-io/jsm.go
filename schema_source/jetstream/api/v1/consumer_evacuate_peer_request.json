{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nats.io/schemas/jetstream/api/v1/consumer_evacuate_peer_request.json",
  "description": "A request to the JetStream $JS.API.CONSUMER.PEER.EVACUATE API",
  "title": "io.nats.jetstream.api.v1.consumer_evacuate_peer_request",
  "type": "object",
  "required": ["peer"],
  "additionalProperties": false,
  "properties": {
    "peer": {
      "type": "string",
      "description": "Server name or peer ID of the peer to evacuate",
      "minLength": 1
    }
  }
}
